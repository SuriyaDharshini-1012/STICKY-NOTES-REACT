const Button=(className,id,type,onSubmit,handleSubmit)=>{

    return(
        className={className},
        id={id},
        type={type},
        onSubmit={handleSubmit}
        
)
}
export default Button;